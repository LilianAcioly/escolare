<h1>Pages#home</h1>
<p>Aqui talvez um banner</p>

<div class="container py-5">
  <div class="row">
    <% @products.each do |product|%>
      <%= link_to product_path(product), class:"text-decoration-none" do %>
       <div class="card-category">
          <p><%= product.name %></p>
        </div>
      <% end %>
    <% end %>
  </div>
</div>
